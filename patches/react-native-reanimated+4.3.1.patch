diff --git a/node_modules/react-native-reanimated/android/build.gradle b/node_modules/react-native-reanimated/android/build.gradle
index 8167d16..2a06603 100644
--- a/node_modules/react-native-reanimated/android/build.gradle
+++ b/node_modules/react-native-reanimated/android/build.gradle
@@ -168,7 +168,8 @@ android {
 
         externalNativeBuild {
             cmake {
-                arguments "-DANDROID_STL=c++_shared",
+                arguments "-DCMAKE_SUPPRESS_REGENERATION=ON",
+                        "-DANDROID_STL=c++_shared",
                         "-DREACT_NATIVE_MINOR_VERSION=${REACT_NATIVE_MINOR_VERSION}",
                         "-DANDROID_TOOLCHAIN=clang",
                         "-DREACT_NATIVE_DIR=${toPlatformFileString(reactNativeRootDir.path)}",
@@ -188,6 +189,12 @@ android {
         cmake {
             version = System.getenv("CMAKE_VERSION") ?: "3.22.1"
             path "CMakeLists.txt"
+            // Windows MAX_PATH workaround: relocate the deep .cxx build dir to a short root
+            // so object paths (which mirror the absolute source path) stay under 260 chars.
+            // Only on Windows; other OSes have no path-length limit and keep the default.
+            if (System.getProperty("os.name").toLowerCase().contains("win")) {
+                buildStagingDirectory "C:/wkr"
+            }
         }
     }
     buildTypes {
